{
  "name": "lanky-services",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs",
    "dev": "npx --yes serve -l 8080 .",
    "preview": "npx --yes serve -l 8080 dist"
  }
}
